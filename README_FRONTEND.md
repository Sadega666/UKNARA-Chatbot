Quick frontend README:

- folder: frontend/
- run: cd frontend && npm install && npm run dev
- Rasa endpoint: http://localhost:5005/webhooks/rest/webhook
- For CORS during development: run Rasa with --cors "*"
